# Day 9 - REST API Implementation

This Projects REST API is built using Node.js and Express.

The API is implemented by following the OpenAPI YAML specification created in Day 4.

Implemented endpoints:

* GET /projects
* POST /projects
* GET /projects/:id
* PATCH /projects/:id/status

Used in-memory storage, request validation, error handling, CORS, and request logging middleware .
